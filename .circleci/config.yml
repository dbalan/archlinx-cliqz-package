jobs:
  build:
    docker:
      - image: base/archlinux:latest
    steps:
      - checkout
      - run:
          name: Update package cache
          command: bash build_package.sh
      - run:
          name: Print the Current Time
          command: date
